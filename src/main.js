import './style.css'

const app = document.querySelector('#app')
app.innerHTML = `
  <div class="page">
    <h1>Vite Hosting Test</h1>
    <p>This is a minimal Vite app scaffold for hosting validation.</p>
    <p>Run <code>npm install</code> and <code>npm run dev</code> to start the dev server.</p>
    <p>For production deployment, run <code>npm run build</code> to generate the dist folder.</p>
    <p>This project is configured for GitHub deployments on Celestine Hosting.</p>
  </div>
`